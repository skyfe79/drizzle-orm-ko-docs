---
title: Drizzle Kit v0.23.2 릴리스
pubDate: 2024-08-05
description: 버그 수정
---

- PostgreSQL에서 `schemaFilter` 객체가 전달될 때 `push` 및 `introspect` 기능에서 발생하던 버그를 수정했습니다. 이전에는 `schemaFilter`에 정의되지 않은 스키마에서도 열거형(enum)을 감지하는 문제가 있었습니다.
- `drizzle-kit up` 커맨드가 시퀀스 릴리스부터 예상대로 동작하도록 수정했습니다.


